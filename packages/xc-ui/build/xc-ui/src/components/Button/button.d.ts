import { BaseButtonProps } from './constants';
/**
 * 这是一个button组件啊
 */
export declare const Button: ({ className, btnType, disabled, size, children, href, ...restProps }: BaseButtonProps) => import("react").JSX.Element;
