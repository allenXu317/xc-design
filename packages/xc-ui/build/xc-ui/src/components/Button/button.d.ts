import { BaseButtonProps } from './constants';
/**
 * 这是一个button组件啊1
 */
export declare const Button: ({ className, btnType, disabled, size, children, href, ...restProps }: BaseButtonProps) => import("react/jsx-runtime").JSX.Element;
