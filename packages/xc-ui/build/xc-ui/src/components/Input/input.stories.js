import { Input } from "./input";
var inputMeta = {
    title: "Input",
    component: Input,
};
export default inputMeta;
export var Default = {
    args: {
        placeholder: "placeholder",
        prepend: "prepend",
        append: "append",
    },
};
